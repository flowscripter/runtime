import InMemoryRegistry from '../src/InMemoryRegistry';

describe('InMemoryRegistry test', () => {

    test('InMemoryRegistry is instantiable', () => {
        expect(new InMemoryRegistry()).toBeInstanceOf(InMemoryRegistry);
    });

    // TODO: test
});
