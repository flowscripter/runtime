import NodeRuntime from '../src/NodeRuntime';

describe('NodeRuntime test', () => {

    test('NodeRuntime is instantiable', () => {
        expect(new NodeRuntime()).toBeInstanceOf(NodeRuntime);
    });

    // TODO: test
});
